*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db
# --- AGREGAR ESTO PARA EVITAR SUBIR CACHÉ ---

# Ignorar archivos cacheados del framework (Vistas compiladas, Sesiones, etc)
/storage/framework/cache/data/*
/storage/framework/sessions/*
/storage/framework/testing/*
/storage/framework/views/*

# Ignorar logs específicos de Laravel (aunque tienes *.log, esto asegura la carpeta)
/storage/logs/*

# Ignorar la caché de arranque (Bootstrap cache) - IMPORTANTE
/bootstrap/cache/*

# PERO... Mantener los archivos .gitignore dentro de esas carpetas
# (Esto sirve para que la carpeta vacía SÍ se suba, pero su contenido basura NO)
!/storage/framework/sessions/.gitignore
!/storage/framework/views/.gitignore
!/storage/framework/cache/data/.gitignore
!/bootstrap/cache/.gitignore